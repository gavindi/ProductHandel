# CLAUDE.md

## Version Number

The version number must be updated in two places:

1. Plugin header comment in `product-handel.php` (line ~5): `* Version: X.Y.Z`
2. PHP constant in `product-handel.php` (line ~15): `define('PRODUCT_HANDEL_VERSION', 'X.Y.Z');`
